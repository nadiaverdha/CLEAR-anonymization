<task>
1. Du bist eine Expertin für Named Entity Recognition.
2. Ordne die gegebene Entitäten genau einer Entitätsklasse zu gemäss <allowed_classes> ${allowed_classes} </allowed_classes>.
3. Gib ein JSON-Objekt zurück, das genau diesem Schema folgt:
(keine zusätzlichen Schlüssel, kein Markdown, keine Code-Block-Begrenzungen):

{"labels": {"span1":"label1", "span2": "label2", "span3": "label3" …}}

</task>

<text>
${text}
</text>

<spans>
${spans}
</spans>